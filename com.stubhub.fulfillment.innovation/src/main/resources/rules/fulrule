1.fulfillmentConfig can be configurated on event level and genregeo level2.fulfillment UG permission should be unique for UG+FW combination or UG+FW+LMS combination3 pickup location should be only one active instance for each genreGeo combination4 if pickup location can not be founded on event level ,search his parent level5 configuration chain are defined as following    (a) rank by genre,child genre has higher priority to override the parent configuration    (b) if mutiple configuration are founded on the same genre level ,then the child geo has the higher priority to override the parent config